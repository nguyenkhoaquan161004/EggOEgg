import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const HandleExchangeReturn = () => {
    return (
        <View>
            <Text>HandleExchangeReturn</Text>
        </View>
    )
}

export default HandleExchangeReturn

const styles = StyleSheet.create({})